{
  "id": "transitions_phase3_assessment",
  "tags": [
    "transitions",
    "assessment"
  ],
  "description": "Phase 4: Candidate finished max subarray with Kadane's, tests passed, walkthrough done. Agent should enter assessment mode with direct follow-up questions then close.",
  "simulated_user": {
    "prompt": "You've finished the max subarray sum problem using Kadane's algorithm. All tests passed, you walked through the code. Now you're waiting for the interviewer's assessment questions. Answer whatever they ask — complexity, design, extensions. When they close, accept it.",
    "first_message": "Alright, so that's the full solution with Kadane's algorithm — single pass, linear time. What else do you want to go over?"
  },
  "new_turns_limit": 8,
  "tool_mocks": {
    "get_current_code": {
      "default_return_value": {
        "code": "def max_subarray_sum(nums):\n    max_so_far = nums[0]\n    max_ending = nums[0]\n    for i in range(1, len(nums)):\n        max_ending = max(nums[i], max_ending + nums[i])\n        max_so_far = max(max_so_far, max_ending)\n    return max_so_far",
        "language": "python",
        "remaining_minutes": 10,
        "hintCount": 0
      }
    },
    "run_code_against_tests": {
      "default_return_value": {
        "passed": 8,
        "failed": 0,
        "total": 8,
        "failed_details": []
      }
    }
  },
  "dynamic_variables": {
    "remaining_minutes": "10"
  },
  "evaluation_criteria": [
    {
      "id": "enters_assessment_mode",
      "name": "Moves to assessment naturally",
      "conversation_goal_prompt": "Agent shifted to assessment mode: asked direct follow-up questions (complexity, design choices, extensions) rather than staying in implementation mode. PASS if behavior reflected assessment-phase mindset with direct questions. FAIL if agent asked to see/walk through code again, or stayed silent, or skipped to close without questions."
    },
    {
      "id": "phase4_decisive_close",
      "name": "Closes after assessment",
      "conversation_goal_prompt": "After asking 1-2 follow-up questions, agent delivered a clear closing line and called end_call. PASS if closed decisively. FAIL if never closed or kept asking questions."
    }
  ],
  "target_phase": 4,
  "target_phase_label": "Phase 4: Assessment & Close",
  "warm_up": {
    "candidate_turns": [
      "I'll use Kadane's algorithm — single pass tracking current and max sum.",
      "Done coding. All tests are passing.",
      "Sure. I initialize both current_sum and max_sum to the first element. Then for each subsequent element, current_sum becomes the max of the element alone or element plus previous current_sum. Max_sum tracks the largest current_sum seen. O(n) time, O(1) space."
    ],
    "target_phase_label": "Phase 4"
  }
}
