{
  "id": "done_asks_walkthrough",
  "tags": [
    "done",
    "communication"
  ],
  "description": "Tests that on 'I'm done', agent asks for a walk-through rather than recapping/verdicting. Agent should be in implementation phase, candidate just finished coding.",
  "simulated_user": {
    "prompt": "You're a candidate who has been coding a solution. You've just finished writing it and you ask the interviewer to take a look.",
    "first_message": "Okay, I think I'm done. Can you take a look?"
  },
  "new_turns_limit": 4,
  "tool_mocks": {
    "get_current_code": {
      "default_return_value": {
        "code": "def max_subarray_sum(nums):\n    max_so_far = nums[0]\n    max_ending = nums[0]\n    for i in range(1, len(nums)):\n        max_ending = max(nums[i], max_ending + nums[i])\n        max_so_far = max(max_so_far, max_ending)\n    return max_so_far",
        "language": "python",
        "remaining_minutes": 15,
        "hintCount": 0
      }
    },
    "run_code_against_tests": {
      "default_return_value": {
        "passed": 8,
        "failed": 0,
        "total": 8,
        "failed_details": [],
        "remainingMinutes": 15
      }
    }
  },
  "dynamic_variables": {
    "remaining_minutes": "15"
  },
  "evaluation_criteria": [
    {
      "id": "asks_walkthrough",
      "name": "Asks them to explain",
      "conversation_goal_prompt": "The agent asked the candidate to walk through their solution, rather than summarizing the code back, reading it aloud, or giving a verdict like 'looks correct'. PASS if it invited the candidate to explain."
    },
    {
      "id": "no_recap_verdict",
      "name": "No code recap or verdict",
      "conversation_goal_prompt": "The agent did NOT narrate/summarize what the code does and did NOT give a correctness verdict. PASS if it avoided both."
    }
  ],
  "target_phase": 2,
  "target_phase_label": "Phase 2: Implementation",
  "warm_up": {
    "candidate_turns": [
      "I'll use a brute force approach — nested loops to check every subarray. Simple but it'll work.",
      "Alright, let me code this up."
    ],
    "target_phase_label": "Phase 2"
  }
}