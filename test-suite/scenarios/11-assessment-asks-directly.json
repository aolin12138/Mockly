{
  "id": "assessment_asks_directly",
  "tags": [
    "assessment",
    "leads"
  ],
  "description": "Phase 4: Candidate has finished coding, all tests passed, walkthrough done. Agent should ask direct bonus questions (complexity, design) then close decisively.",
  "simulated_user": {
    "prompt": "You've just finished the max subarray sum problem. All tests passed. You walked through your Kadane's algorithm approach. Now you're waiting for the interviewer's wrap-up questions. Answer briefly whatever they ask — complexity, design choices, extensions. When they give a closing signal, accept it.",
    "first_message": "Alright, so that's the full solution with Kadane's algorithm — single pass, O(n). What else do you want to go over?"
  },
  "new_turns_limit": 8,
  "tool_mocks": {
    "get_current_code": {
      "default_return_value": {
        "code": "def max_subarray_sum(nums):\n    max_so_far = nums[0]\n    max_ending = nums[0]\n    for i in range(1, len(nums)):\n        max_ending = max(nums[i], max_ending + nums[i])\n        max_so_far = max(max_so_far, max_ending)\n    return max_so_far",
        "language": "python",
        "remaining_minutes": 12,
        "hintCount": 0
      }
    },
    "run_code_against_tests": {
      "default_return_value": {
        "passed": 8,
        "failed": 0,
        "total": 8,
        "failed_details": []
      }
    }
  },
  "dynamic_variables": {
    "remaining_minutes": "12"
  },
  "evaluation_criteria": [
    {
      "id": "phase4_sequence",
      "name": "Follows Phase 4 protocol",
      "conversation_goal_prompt": "Agent followed the Phase 4 sequence: (1) asked for walkthrough or acknowledged it was done, (2) asked 1-2 direct bonus questions (complexity, design choices), (3) gave a clear closing line ('That is all for today') and called end_call. PASS if the 3-step sequence was followed. FAIL if the agent skipped steps, asked permission ('do you want to discuss complexity?'), or never closed."
    },
    {
      "id": "direct_questions",
      "name": "Leads with direct questions",
      "conversation_goal_prompt": "Agent asked DIRECT questions ('What's the time complexity?' 'Why this approach?') rather than offering menus ('Do you want to discuss complexity?'). PASS if questions were direct and confident. FAIL if agent asked permission or presented options."
    },
    {
      "id": "no_direct_verdict",
      "name": "No verdict",
      "conversation_goal_prompt": "Agent did NOT say 'great job,' 'well done,' 'nice work,' 'solid,' 'good approach,' 'great choice.' PASS if none of these appeared."
    }
  ],
  "target_phase": 4,
  "target_phase_label": "Phase 4: Assessment & Close",
  "warm_up": {
    "candidate_turns": [
      "I'll use Kadane's algorithm — track running sum and max sum in one pass.",
      "Okay, done coding. All tests are passing.",
      "Sure. I start with both current sum and max sum at the first element. Then loop from index 1 — at each step, current sum is either the element alone or the element plus previous current sum. Max sum updates whenever current sum is larger."
    ],
    "target_phase_label": "Phase 4"
  }
}
